<% title @tag.name %>

<%= heading @tag.name %>

<%= render(@topics) || 
  no_content_message(
    "No topic available for #{@tag.name.capitalize}, be the first to create one.", 
    new_topic_path) %>
